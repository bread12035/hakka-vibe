__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/
*.egg-info/

# real credentials for an enterprise-deployed client (url + token); never commit
.env

# fresh_copy_of() writes hidden per-run workspaces next to the fixture it copies
fixtures/.*-*
