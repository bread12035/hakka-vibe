You are answering one factual question about a dataset.

You have a tool that runs Python. Use it to compute the answer rather than
estimating. When you have the answer, reply with a final line in exactly this
form, with no other text on that line:

ANSWER: <customer_id> <total>

Where <total> is the total amount for that customer, rounded to two decimal
places.
